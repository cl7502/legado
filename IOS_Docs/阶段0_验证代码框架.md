# 阶段 0：验证代码框架

## 代码框架说明

本文档包含用于在 macOS 环境下验证 iOS 版本兼容性的代码框架。这些代码需要在 Xcode 中创建并运行。

---

## 1. JavaScriptCore 基础功能测试

### 1.1 创建测试项目

**步骤**：
1. 打开 Xcode
2. 创建新的 macOS App 项目（Command Line Tool）
3. 配置项目：
   - Product Name: LegadoValidation
   - Language: Swift
   - Minimum macOS Version: 13.0

### 1.2 JavaScriptCore 基础测试代码

**文件**：`JavaScriptCoreTests.swift`

```swift
import Foundation
import JavaScriptCore

class JavaScriptCoreTests {
    let context: JSContext

    init() {
        self.context = JSContext()
        setupContext()
    }

    private func setupContext() {
        // 配置异常处理
        context.exceptionHandler = { context, exception in
            print("JavaScript Error: \(exception ?? "")")
        }
    }

    // 测试 1: 基础算术运算
    func testArithmeticOperations() -> Bool {
        let result = context.evaluateScript("1 + 1")
        let success = result?.toInt32() == 2
        print("✅ 测试 1 (算术运算): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 2: 函数调用
    func testFunctionCall() -> Bool {
        let result = context.evaluateScript("function add(a, b) { return a + b; } add(1, 2)")
        let success = result?.toInt32() == 3
        print("✅ 测试 2 (函数调用): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 3: 对象操作
    func testObjectOperation() -> Bool {
        let result = context.evaluateScript("var obj = {name: 'test'}; obj.name")
        let success = result?.toString() == "test"
        print("✅ 测试 3 (对象操作): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 4: 数组操作
    func testArrayOperation() -> Bool {
        let result = context.evaluateScript("var arr = [1, 2, 3]; arr.length")
        let success = result?.toInt32() == 3
        print("✅ 测试 4 (数组操作): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 5: 字符串操作
    func testStringOperation() -> Bool {
        let result = context.evaluateScript("'hello'.length")
        let success = result?.toInt32() == 5
        print("✅ 测试 5 (字符串操作): \(success ? "通过" : "失败")")
        return success
    }

    // 运行所有测试
    func runAllTests() -> [String: Bool] {
        var results: [String: Bool] = [:]

        results["算术运算"] = testArithmeticOperations()
        results["函数调用"] = testFunctionCall()
        results["对象操作"] = testObjectOperation()
        results["数组操作"] = testArrayOperation()
        results["字符串操作"] = testStringOperation()

        return results
    }
}
```

---

## 2. Rhino API 兼容性测试

### 2.1 Rhino API 兼容层代码

**文件**：`JSJavaHelper.swift`

```swift
import Foundation
import JavaScriptCore

@objc protocol JSJavaHelperProtocol: JSExport {
    func ajax(_ url: String, method: String) -> String?
    func put(_ key: String, value: Any)
    func get(_ key: String) -> Any?
    func log(_ message: String)
}

class JSJavaHelper: NSObject, JSJavaHelperProtocol {
    func ajax(_ url: String, method: String = "GET") -> String? {
        print("AJAX Request: \(url), Method: \(method)")
        // 模拟网络请求
        return "mock_response"
    }

    func put(_ key: String, value: Any) {
        print("PUT: \(key) = \(value)")
        UserDefaults.standard.set(value, forKey: key)
    }

    func get(_ key: String) -> Any? {
        print("GET: \(key)")
        return UserDefaults.standard.value(forKey: key)
    }

    func log(_ message: String) {
        print("LOG: \(message)")
    }
}
```

### 2.2 Rhino API 测试代码

**文件**：`RhinoAPITests.swift`

```swift
import Foundation
import JavaScriptCore

class RhinoAPITests {
    let context: JSContext

    init() {
        self.context = JSContext()
        setupContext()
    }

    private func setupContext() {
        // 配置异常处理
        context.exceptionHandler = { context, exception in
            print("JavaScript Error: \(exception ?? "")")
        }

        // 注入 Rhino API 兼容层
        let javaHelper = JSJavaHelper()
        context.globalObject.setValue(javaHelper, forProperty: "java")
    }

    // 测试 1: ajax 方法
    func testAjaxMethod() -> Bool {
        let result = context.evaluateScript("java.ajax('https://example.com', 'GET')")
        let success = result?.toString() == "mock_response"
        print("✅ 测试 1 (ajax 方法): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 2: put/get 方法
    func testPutGetMethod() -> Bool {
        context.evaluateScript("java.put('testKey', 'testValue')")
        let result = context.evaluateScript("java.get('testKey')")
        let success = result?.toString() == "testValue"
        print("✅ 测试 2 (put/get 方法): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 3: log 方法
    func testLogMethod() -> Bool {
        let result = context.evaluateScript("java.log('test message')")
        let success = result != nil
        print("✅ 测试 3 (log 方法): \(success ? "通过" : "失败")")
        return success
    }

    // 运行所有测试
    func runAllTests() -> [String: Bool] {
        var results: [String: Bool] = [:]

        results["ajax 方法"] = testAjaxMethod()
        results["put/get 方法"] = testPutGetMethod()
        results["log 方法"] = testLogMethod()

        return results
    }
}
```

---

## 3. 规则解析系统测试

### 3.1 规则类型定义

**文件**：`RuleType.swift`

```swift
import Foundation

enum RuleType {
    case defaultRule
    case xpath
    case json
    case regex
    case javascript

    static func parse(_ rule: String) -> (RuleType, String) {
        if rule.hasPrefix("<js>") {
            let content = String(rule.dropFirst(4).dropLast(5))
            return (.javascript, content)
        } else if rule.hasPrefix("@XPath:") {
            let content = String(rule.dropFirst(7))
            return (.xpath, content)
        } else if rule.hasPrefix("@Json:") {
            let content = String(rule.dropFirst(6))
            return (.json, content)
        } else if rule.hasPrefix("@Regex:") {
            let content = String(rule.dropFirst(7))
            return (.regex, content)
        } else if rule.hasPrefix("@") {
            let content = String(rule.dropFirst(1))
            return (.defaultRule, content)
        } else {
            return (.defaultRule, rule)
        }
    }
}
```

### 3.2 规则解析器测试

**文件**：`RuleParserTests.swift`

```swift
import Foundation

class RuleParserTests {
    // 测试 1: XPath 规则
    func testXPathRule() -> Bool {
        let rule = "@XPath://div[@class='title']/text()"
        let (type, content) = RuleType.parse(rule)
        let success = type == .xpath && content == "//div[@class='title']/text()"
        print("✅ 测试 1 (XPath 规则): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 2: JSON 规则
    func testJSONRule() -> Bool {
        let rule = "@Json:$.data.list[*].title"
        let (type, content) = RuleType.parse(rule)
        let success = type == .json && content == "$.data.list[*].title"
        print("✅ 测试 2 (JSON 规则): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 3: 正则规则
    func testRegexRule() -> Bool {
        let rule = "@Regex:<title>(.*?)</title>"
        let (type, content) = RuleType.parse(rule)
        let success = type == .regex && content == "<title>(.*?)</title>"
        print("✅ 测试 3 (正则规则): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 4: JavaScript 规则
    func testJavaScriptRule() -> Bool {
        let rule = "<js>result.map(item => item.title)</js>"
        let (type, content) = RuleType.parse(rule)
        let success = type == .javascript && content == "result.map(item => item.title)"
        print("✅ 测试 4 (JavaScript 规则): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 5: 默认规则
    func testDefaultRule() -> Bool {
        let rule = ".title@text"
        let (type, content) = RuleType.parse(rule)
        let success = type == .defaultRule && content == ".title@text"
        print("✅ 测试 5 (默认规则): \(success ? "通过" : "失败")")
        return success
    }

    // 运行所有测试
    func runAllTests() -> [String: Bool] {
        var results: [String: Bool] = [:]

        results["XPath 规则"] = testXPathRule()
        results["JSON 规则"] = testJSONRule()
        results["正则规则"] = testRegexRule()
        results["JavaScript 规则"] = testJavaScriptRule()
        results["默认规则"] = testDefaultRule()

        return results
    }
}
```

---

## 4. HTML 解析测试

### 4.1 HTML 解析器代码

**文件**：`HTMLParser.swift`

```swift
import Foundation

class HTMLParser {
    // CSS 选择器解析
    func css(_ html: String, css: String) -> String? {
        // 这里需要集成 SwiftSoup
        // 由于当前环境限制，这里只是框架代码
        print("CSS 解析: \(css)")
        return nil
    }

    // XPath 解析
    func xpath(_ html: String, xpath: String) -> String? {
        // 这里需要集成 SwiftSoup
        print("XPath 解析: \(xpath)")
        return nil
    }

    // JSONPath 解析
    func jsonPath(_ json: String, jsonPath: String) -> String? {
        // 这里需要实现 JSONPath
        print("JSONPath 解析: \(jsonPath)")
        return nil
    }

    // 正则解析
    func regex(_ html: String, regex: String) -> String? {
        guard let regex = try? NSRegularExpression(pattern: regex) else {
            return nil
        }
        let range = NSRange(html.startIndex..., in: html)
        guard let match = regex.firstMatch(in: html, range: range),
              let range = match.range(at: 1) else {
            return nil
        }
        return (html as NSString).substring(with: range)
    }
}
```

### 4.2 HTML 解析器测试

**文件**：`HTMLParserTests.swift`

```swift
import Foundation

class HTMLParserTests {
    let parser = HTMLParser()

    // 测试 1: CSS 选择器
    func testCSSSelector() -> Bool {
        let html = "<html><body><div class='title'>Test</div></body></html>"
        let result = parser.css(html, css: ".title")
        let success = result == "Test"
        print("✅ 测试 1 (CSS 选择器): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 2: XPath
    func testXPath() -> Bool {
        let html = "<html><body><div class='title'>Test</div></body></html>"
        let result = parser.xpath(html, xpath: "//div[@class='title']/text()")
        let success = result == "Test"
        print("✅ 测试 2 (XPath): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 3: JSONPath
    func testJSONPath() -> Bool {
        let json = "{\"data\": {\"title\": \"Test\"}}"
        let result = parser.jsonPath(json, jsonPath: "$.data.title")
        let success = result == "Test"
        print("✅ 测试 3 (JSONPath): \(success ? "通过" : "失败")")
        return success
    }

    // 测试 4: 正则
    func testRegex() -> Bool {
        let html = "<title>Test Title</title>"
        let result = parser.regex(html, regex: "<title>(.*?)</title>")
        let success = result == "Test Title"
        print("✅ 测试 4 (正则): \(success ? "通过" : "失败")")
        return success
    }

    // 运行所有测试
    func runAllTests() -> [String: Bool] {
        var results: [String: Bool] = [:]

        results["CSS 选择器"] = testCSSSelector()
        results["XPath"] = testXPath()
        results["JSONPath"] = testJSONPath()
        results["正则"] = testRegex()

        return results
    }
}
```

---

## 5. 主程序入口

### 5.1 main.swift

**文件**：`main.swift`

```swift
import Foundation

print("========================================")
print("  Legado iOS 版本验证程序")
print("  版本: v1.0")
print("  日期: 2025年12月28日")
print("========================================\n")

// JavaScriptCore 基础功能测试
print("【1. JavaScriptCore 基础功能测试】")
let jsTests = JavaScriptCoreTests()
let jsResults = jsTests.runAllTests()
printResults(jsResults, title: "JavaScriptCore 基础功能")

// Rhino API 兼容性测试
print("\n【2. Rhino API 兼容性测试】")
let rhinoTests = RhinoAPITests()
let rhinoResults = rhinoTests.runAllTests()
printResults(rhinoResults, title: "Rhino API 兼容性")

// 规则解析系统测试
print("\n【3. 规则解析系统测试】")
let ruleTests = RuleParserTests()
let ruleResults = ruleTests.runAllTests()
printResults(ruleResults, title: "规则解析系统")

// HTML 解析测试
print("\n【4. HTML 解析测试】")
let htmlTests = HTMLParserTests()
let htmlResults = htmlTests.runAllTests()
printResults(htmlResults, title: "HTML 解析")

// 汇总结果
print("\n========================================")
print("  测试结果汇总")
print("========================================")

let allResults = [
    ("JavaScriptCore 基础功能", jsResults),
    ("Rhino API 兼容性", rhinoResults),
    ("规则解析系统", ruleResults),
    ("HTML 解析", htmlResults)
]

var totalTests = 0
var passedTests = 0

for (category, results) in allResults {
    let categoryTotal = results.count
    let categoryPassed = results.values.filter { $0 }.count
    let passRate = Double(categoryPassed) / Double(categoryTotal) * 100

    totalTests += categoryTotal
    passedTests += categoryPassed

    print("\(category): \(categoryPassed)/\(categoryTotal) 通过 (\(String(format: "%.1f", passRate))%)")
}

let overallPassRate = Double(passedTests) / Double(totalTests) * 100
print("\n总计: \(passedTests)/\(totalTests) 通过 (\(String(format: "%.1f", overallPassRate))%)")

if overallPassRate >= 95 {
    print("\n✅ 验证通过！可以继续开发。")
} else if overallPassRate >= 80 {
    print("\n⚠️ 验证部分通过，需要调整方案。")
} else {
    print("\n❌ 验证失败，需要重新评估技术方案。")
}

print("========================================")

// 辅助函数：打印结果
func printResults(_ results: [String: Bool], title: String) {
    print("\n\(title)测试结果:")
    for (testName, passed) in results {
        let status = passed ? "✅" : "❌"
        print("  \(status) \(testName)")
    }
}
```

---

## 6. 项目配置

### 6.1 Package.swift（如果使用 SPM）

**文件**：`Package.swift`

```swift
// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "LegadoValidation",
    platforms: [
        .macOS(.v13)
    ],
    dependencies: [
        // 如果需要，可以添加其他依赖
    ],
    targets: [
        .executableTarget(
            name: "LegadoValidation",
            dependencies: []
        )
    ]
)
```

### 6.2 Xcode 项目配置

**步骤**：
1. 创建新的 macOS App 项目
2. 添加所有 Swift 文件
3. 配置项目设置：
   - Deployment Target: macOS 13.0
   - Swift Language Version: Swift 5.9

---

## 7. 运行测试

### 7.1 在 Xcode 中运行

**步骤**：
1. 打开 Xcode 项目
2. 选择 `LegadoValidation` scheme
3. 点击运行按钮（⌘ + R）
4. 查看控制台输出

### 7.2 预期输出

```
========================================
  Legado iOS 版本验证程序
  版本: v1.0
  日期: 2025年12月28日
========================================

【1. JavaScriptCore 基础功能测试】
✅ 测试 1 (算术运算): 通过
✅ 测试 2 (函数调用): 通过
✅ 测试 3 (对象操作): 通过
✅ 测试 4 (数组操作): 通过
✅ 测试 5 (字符串操作): 通过

JavaScriptCore 基础功能测试结果:
  ✅ 算术运算
  ✅ 函数调用
  ✅ 对象操作
  ✅ 数组操作
  ✅ 字符串操作

【2. Rhino API 兼容性测试】
✅ 测试 1 (ajax 方法): 通过
✅ 测试 2 (put/get 方法): 通过
✅ 测试 3 (log 方法): 通过

Rhino API 兼容性测试结果:
  ✅ ajax 方法
  ✅ put/get 方法
  ✅ log 方法

【3. 规则解析系统测试】
✅ 测试 1 (XPath 规则): 通过
✅ 测试 2 (JSON 规则): 通过
✅ 测试 3 (正则规则): 通过
✅ 测试 4 (JavaScript 规则): 通过
✅ 测试 5 (默认规则): 通过

规则解析系统测试结果:
  ✅ XPath 规则
  ✅ JSON 规则
  ✅ 正则规则
  ✅ JavaScript 规则
  ✅ 默认规则

【4. HTML 解析测试】
❌ 测试 1 (CSS 选择器): 失败
❌ 测试 2 (XPath): 失败
❌ 测试 3 (JSONPath): 失败
✅ 测试 4 (正则): 通过

HTML 解析测试结果:
  ❌ CSS 选择器
  ❌ XPath
  ❌ JSONPath
  ✅ 正则

========================================
  测试结果汇总
========================================
JavaScriptCore 基础功能: 5/5 通过 (100.0%)
Rhino API 兼容性: 3/3 通过 (100.0%)
规则解析系统: 5/5 通过 (100.0%)
HTML 解析: 1/4 通过 (25.0%)

总计: 14/17 通过 (82.4%)

⚠️ 验证部分通过，需要调整方案。
========================================
```

---

## 8. 后续步骤

### 8.1 如果验证通过（≥95%）

1. 继续阶段 1：项目初始化
2. 开始正式开发

### 8.2 如果部分通过（80-94%）

1. 分析失败原因
2. 调整技术方案
3. 重新验证

### 8.3 如果验证失败（<80%）

1. 重新评估技术方案
2. 考虑替代方案（如 QuickJS）
3. 重新进行验证

---

## 9. 注意事项

1. **环境要求**：
   - macOS 13.0+
   - Xcode 15.0+
   - Swift 5.9+

2. **依赖管理**：
   - 如果需要使用 SwiftSoup，需要通过 SPM 或 CocoaPods 安装
   - 如果需要使用 Alamofire，同样需要通过包管理器安装

3. **测试数据**：
   - 准备足够的测试数据
   - 包括各种类型的规则
   - 包括边界情况

4. **性能测试**：
   - 测试大量数据的处理
   - 测试复杂规则的执行
   - 测试内存使用情况

---

**文档更新时间**：2025年12月28日
**版本**：v1.0